// const color = document.querySelector(`.p`);
// color.addEventListener(`mousemove`,function(){
//    color.style.color = `white`;
// })

const chat = document.querySelector(`.makefreebet`).style.cursor = `pointer`;
